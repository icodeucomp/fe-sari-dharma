on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    # environment: cpanel

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: Set Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🏗️ Build Next.js app
        run: npm run build

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.saridharma.id
          username: frontend@saridharma.id
          password: o6&4n&H5y&;G(ofM
          protocol: ftps
          local-dir: .next/
          server-dir: home/saridar/sari_dharma/frontend/main/
